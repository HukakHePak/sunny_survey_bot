export enum CommandNames {
  Start = 'start',
  Add = 'add',
  Stats = 'stats',
  Update = 'update',
  Results = 'results',
  RepeatVote = 'repeat_vote',
  Survey = 'survey',
  Vipe = 'vipe',
  List = 'list',
  Remove = 'remove',
}

export default CommandNames;

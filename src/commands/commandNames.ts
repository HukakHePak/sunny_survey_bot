export enum CommandNames {
  Start = 'start',
  Add = 'add',
  Results = 'results',
  RepeatVote = 'repeat_vote',
  Survey = 'survey',
  List = 'list',
  Remove = 'remove',
}

export default CommandNames;
